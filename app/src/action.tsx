export const computerAction = {
    init: (() => { }),
    nfaToDfa: (() => { }),
    minimizeDfa: (() => { }),
    mooreToMealy: (() => { }),
    mealyToMoore: (() => { })
}

export const controlAction = {
    changerByStack: (() => { }),
    run: (() => { }),
    step: (() => { }),
    reset: (() => { }),
}